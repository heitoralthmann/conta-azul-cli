name: Security

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  schedule:
    # Catches a CVE published in an existing dependency after the last commit.
    - cron: '0 9 * * 1' # Every Monday 9am UTC
  # Reutilizável: chamado por `release.yml` para que uma tag não publique um
  # artefato construído sobre dependência com vulnerabilidade conhecida.
  workflow_call:

# Somente leitura: nenhum destes jobs escreve no repositório.
# Explícito em vez de herdar o default do repo, que é uma
# configuração que pode mudar sem ninguém revisar os workflows.
permissions:
  contents: read

jobs:
  audit:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Setup PHP
        uses: shivammathur/setup-php@f3e473d116dcccaddc5834248c87452386958240 # v2.37.2
        with:
          php-version: '8.4'
          extensions: mbstring, openssl, posix
          coverage: none

      - name: Install dependencies
        run: composer install --no-interaction --prefer-dist --optimize-autoloader

      - name: Validate composer.json and composer.lock
        run: composer validate --strict

      - name: Audit dependencies for known vulnerabilities
        run: composer audit
